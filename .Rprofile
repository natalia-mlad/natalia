if (interactive()) {
  suppressMessages(require(fs))
  suppressMessages(require(magrittr))
  suppressMessages(require(devtools))
  suppressMessages(require(usethis))
  suppressMessages(require(attachment))
}
options(scipen = 9999, crayon.enabled = TRUE)
